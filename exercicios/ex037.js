// Exercício 037: Desestruturação (Destructuring) de Objetos
// Objetivo: Extrair valores de propriedades de objetos diretamente para variáveis.
//
// Instruções:
// 1. Dado o objeto: `const config = { url: 'localhost', porta: 8080 };`
// 2. Use a desestruturação para criar as variáveis `url`, `port` (renomeando a propriedade `porta`) e `timeout` (com o valor padrão `3000`).
// 3. Imprima os valores no console no formato: "URL: [url], Porta: [port], Timeout: [timeout]".
//
// Saída esperada no terminal:
// URL: localhost, Porta: 8080, Timeout: 3000
//
// Para executar este arquivo, use o comando: node ex037.js
//
// ====================================================================
// EXEMPLO SIMILAR / CONCEITO:
//
// Exemplo similar: Extrair dados de perfil de rede social definindo apelido e valores padrão.
//
// const perfil = { username: 'jonas99', followers: 500 };
// const { username: nick, followers: seguidores, posts = 0 } = perfil;
// console.log(nick, seguidores, posts);
// ====================================================================

// Escreva seu código abaixo:


