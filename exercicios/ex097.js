// Exercício 097: Console Avançado
// Objetivo: Organizar logs com formatação visual avançada, medição de timers de código e renderização de tabelas.
//
// Instruções:
// 1. Use `console.time('Timer')` para iniciar a cronometragem de um trecho de código.
// 2. Use `console.group('Grupo de Teste')` para abrir uma identação agrupada de logs e faça prints dentro dela. Feche o grupo com `console.groupEnd()`.
// 3. Crie um array de objetos simples de exemplo e imprima-o em formato de tabela elegante no console usando `console.table()`.
// 4. Finalize o cronômetro imprimindo o tempo medido no terminal com `console.timeEnd('Timer')`.
//
// Saída esperada no terminal:
// (Tabela visual desenhada no console com colunas, seguidos por logs agrupados e contagem de milissegundos)
//
// Para executar este arquivo, use o comando: node ex097.js

// Escreva seu código abaixo:

