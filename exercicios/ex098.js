// Exercício 098: URLSearchParams
// Objetivo: Criar, inspecionar e alterar parâmetros de busca (query strings) usando o construtor utilitário URLSearchParams.
//
// Instruções:
// 1. Crie uma query string inicial instanciando `new URLSearchParams('?busca=node&limite=10')`.
// 2. Imprima o formato string inicial no terminal usando `.toString()`.
// 3. Adicione um novo parâmetro chamado "pagina" com o valor "2" usando o método `.set()` ou `.append()`.
// 4. Verifique se o parâmetro "busca" existe usando o método `.has()`.
// 5. Imprima no terminal a query string final gerada e convertida em string.
//
// Saída esperada no terminal:
// Query string inicial: busca=node&limite=10
// Query string final: busca=node&limite=10&pagina=2
//
// Para executar este arquivo, use o comando: node ex098.js

// Escreva seu código abaixo:

